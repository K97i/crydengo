# Crydengo

## TO-DO

Store per-guild configs with [Keyv](https://discordjs.guide/legacy/keyv/keyv)
Track new members with above database
Auto-mod regex detection
Server invite scanning
Add bot-log with per-guild channel setting

## Run Bot

1. Insert token in `src/config.json`

```json
{
	"token": "[YOUR-TOKEN-HERE]"
}
```

2. Change directory to /src (`cd src`).
3. Run `npm run dev`.